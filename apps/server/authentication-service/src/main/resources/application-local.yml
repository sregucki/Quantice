postgres:
  hostname: "[::1]"
  port: 5433
  databaseName: auth_data
  username: root
  password: root

spring:
  jpa:
    generate-ddl: false
    defer-datasource-initialization: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://${postgres.hostname}:${postgres.port}/${postgres.databaseName}
    username: ${postgres.username}
    password: ${postgres.password}
  sql:
    init:
      mode: always
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 48911344485-673td23d4bcvu2vb4l2mume5lhr20vbp.apps.googleusercontent.com
            client-secret: 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

logging:
  level:
    org.springframework.web: DEBUG