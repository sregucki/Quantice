postgres:
  hostname: "[::1]"
  port: 5433
  databaseName: auth_data
  username: root
  password: root

spring:
  jpa:
    generate-ddl: false
    defer-datasource-initialization: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://${postgres.hostname}:${postgres.port}/${postgres.databaseName}
    username: ${postgres.username}
    password: ${postgres.password}
  sql:
    init:
      mode: always
  security:
    oauth2:
      client:
        registration:
          google:
            redirect-uri: http://localhost:8081/login/oauth2/code/google
            client-id: 48911344485-i1t6bbhfjpd4tuapskk55td70ccghoke.apps.googleusercontent.com
            client-secret: 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

logging:
  level:
    org.springframework.web: DEBUG